package ksa.so.repositories;

import org.springframework.data.jpa.repository.JpaRepository;

import ksa.so.domain.MetaKeyword;

public interface MetaKeywordRepository extends JpaRepository<MetaKeyword, Long> {

}
